docs/build

.DS_Store
build

.claude
plan

# Uncompressed source imaging. scripts/optimize-assets.mjs reads this and
# writes the compressed build into web/public/modelView.
assets-src/

# web/ ignores its own build outputs, node_modules and staging directories
# in web/.gitignore -- do not duplicate them here.
